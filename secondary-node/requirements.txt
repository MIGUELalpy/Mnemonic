# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ── PyTorch — install CUDA 12.4 build to match the system CUDA version ────────
# If this fails, visit https://pytorch.org/get-started/locally/ and select
# your exact CUDA version to get the right index URL.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124 

# ── Embedding model (BGE-M3) ──────────────────────────────────────────────────
FlagEmbedding==1.3.3              # provides BGEM3FlagModel with GPU support

# ── NLI classification model (DeBERTa-v3-large-MNLI) ─────────────────────────
transformers

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy==1.26.4
structlog==24.4.0
